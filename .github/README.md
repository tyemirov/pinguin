# GitHub Automation

GitHub Actions are intentionally disabled for this repository.

Run `make ci` locally for validation. Run `make publish` from a clean `master` branch matching `origin/master` to publish the `linux/amd64` GHCR image and deploy `web/` to the legacy `gh-pages` branch root.
